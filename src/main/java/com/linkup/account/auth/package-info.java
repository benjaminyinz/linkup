/**
 * 认证子域：登录、注册、JWT 凭证。
 *
 * <p>当前仅保留 {@link com.linkup.account.auth.config}（JWT 配置）
 * 与 {@link com.linkup.account.auth.support}（JWT 工具与拦截器）；
 * 业务部分（注册 / 登录 / 凭证表）等待新 API 定稿后按 plan Phase 3 规范重写。</p>
 */
package com.linkup.account.auth;
